import { MeuFiltroImpuroPipe } from './meu-filtro-impuro.pipe';

describe('MeuFiltroImpuroPipe', () => {
  it('create an instance', () => {
    const pipe = new MeuFiltroImpuroPipe();
    expect(pipe).toBeTruthy();
  });
});
