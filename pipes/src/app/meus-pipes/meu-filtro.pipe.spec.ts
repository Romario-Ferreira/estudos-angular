import { MeuFiltroPipe } from './meu-filtro.pipe';

describe('MeuFiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new MeuFiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
